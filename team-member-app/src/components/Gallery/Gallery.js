import React from 'react';
import PropTypes from 'prop-types';


const Gallery = () => (
  <div>
    Gallery Component
  </div>
);

Gallery.propTypes = {};

Gallery.defaultProps = {};

export default Gallery;
